<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Posts</title>
  </head>
  <body>
    <%= "#{@post_name} was deleted" if @post_name %>
    <h1>Posts</h1>
    <% @posts.each do |post| %>
      <h3><%= post.name %></h3>
      <h4><%= post.content %></h4>
    <% end %>
  </body>
</html>
